[build]
  command = "npm run build"
  publish = ".next"
  functions = "Netlify/functions"

[dev]
  framework = "next"
  targetPort = 3000

[functions."cacheDailyData"]
  schedule = "0 0 * * *"
  
[[plugins]]
  package = "./netlify/plugins/run-once"
